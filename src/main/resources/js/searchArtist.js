var artistInput = document.getElementById("artist");

artistInput.addEventListener("keyup", getArtistsList());


function getArtistsList(event) {
    if (event.key === 13 || event.key=="enter") {

    }
}